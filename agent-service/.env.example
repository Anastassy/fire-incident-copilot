# Configuration only; no secret values.
FIRE_ENGINE=fixture
FIRE_DB_PATH=work/runtime.sqlite3
FIRE_WATCH_TIMEOUT_MS=300000
# For SDK execution set FIRE_ENGINE=sdk and supply OPENAI_API_KEY and FIRE_MODEL.

# Optional shared local-demo cookie, supplied by a trusted same-origin proxy.
# FIRE_UI_SESSION_TOKEN=

# Optional OpenRouter provider (load into process environment)
# FIRE_PROVIDER=openrouter
# FIRE_MODEL=openai/gpt-5.6-sol
# OPENROUTER_API_KEY=
